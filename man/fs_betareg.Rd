% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_betareg.R
\name{fs_betareg}
\alias{fs_betareg}
\title{Perform Beta Regression for Factorial Surveys}
\usage{
fs_betareg(formula, data, transform = TRUE, compute_me = TRUE, verbose = TRUE)
}
\arguments{
\item{formula}{A symbolic description of the model to be fitted.}

\item{data}{A data frame containing the variables in the model.}

\item{transform}{A logical value. If TRUE (default), the function assumes the
response variable is on a 0-10 scale and transforms it to the (0, 1) interval
required for beta regression. If FALSE, it assumes the data is already in the
correct format.}

\item{compute_me}{A logical value. If TRUE (default), computes and returns
marginal effects using the \code{margins} package.}

\item{verbose}{A logical value. If TRUE (default), prints the model summary
and fit statistics to the console.}
}
\value{
A list containing the fitted model object (\code{model}) and the computed
marginal effects (\code{marginal_effects}), if requested.
}
\description{
This function is a wrapper for \code{betareg::betareg} tailored for
factorial survey data, often on a 0-10 point scale. It includes an
option to automatically transform the response variable to the (0, 1) interval.
}
\examples{
\dontrun{
  set.seed(123)
  n <- 100
  vignette_data <- data.frame(
    score = rbeta(n, 2, 5) * 10,
    x1 = rnorm(n),
    x2 = factor(sample(0:1, n, replace = TRUE))
  )

  results <- fs_betareg(score ~ x1 + x2, data = vignette_data)
  summary(results$model)
  summary(results$marginal_effects)
}
}
